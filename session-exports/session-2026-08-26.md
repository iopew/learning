# New session — 2026-08-24 → 2026-08-26

**Session ID:** (hand-written export — raw opencode dump not generated; this is a structured resume record)
**Created:** 8/24/2026
**Updated:** 8/26/2026

---

## One-liner

Milestone 5 closed, then split: `main` = tesseract-only skeleton (offline, `sips 1200` + `tesseract -l eng`), `feature/gemini` = Gemini-only direct `data []byte` → `gemini-3.6-flash` ready-JSON (no `sips`, no temp files, any-store prompt). Handler now direct bytes, `go vet` green on both branches, `2026-08-25 15.55.25.jpg` (359×1280 tight crop) finally correct `2026-08-23`.

## Project timeline

1. **Where we started** — `f06db81` milestone 5 mixed (tesseract + Gemini 3.6 Flash ready-JSON, `sips 1200`, 5 hardened parser bites). Last export `session-2026-08-23.md` (`ses_fe25`, updated `8/23 20:04`) still had `W387 LAMP` date damage report.
2. **Branch decision (your call)** — you wanted no mess, every engine on its own branch for easy `git checkout` switching. Agreed: `main` = tesseract-only (local, offline), `feature/gemini` = Gemini improvement (cloud). Not "main = skeleton" — tesseract *is* the skeleton because it's offline and private.
3. **Freeze + diverge (you typed):** `git branch feature/gemini` at `f06db81` → `git push -u expense-tracker feature/gemini` (remote is `expense-tracker`, not `origin` — `origin` gave `fatal: 'origin' does not appear`). `main` stayed at `f06db81` until strip.
4. **Strip `main` to tesseract-only (you typed, I navigated):** `git rm internal/web/gemini.go` + delete the two `geminiExtract` blocks in `handlers.go:173-195` (Family C scanned-PDF) and `226-245` (image) — leave `sips 1200` + `tesseract -l eng` + `usedOCR` date fallback. `git commit -m "main: tesseract-only skeleton — remove Gemini, keep sips 1200 + hardened parser"` → `fa3fb59`, then fix leftover `geminiExtract` calls → `cb8833f fix main: remove leftover Gemini calls — vet green`. `git push -u expense-tracker main` pushed both, GitHub `Compare & pull request` banner dismissed — keep branches diverged.
5. **Make `feature/gemini` Gemini-only (you typed, I commented):** `git checkout feature/gemini` → I commented the two `tesseract` fallback blocks (`199-207` `tmpPng` and `252-260` `tmp`), you deleted the 18 `//` lines. `git commit -m "feature/gemini: Gemini Only - no tesseract"` → `c6379b9`, pushed.
6. **Your question — why mime only `jpeg` fallback:** `http.DetectContentType(data)` already returns `image/png`/`jpeg`/`application/pdf` from magic bytes; `octet-stream` fallback to `jpeg` is just the rare "can't tell" case — `png` would also work, `jpeg` is the phone-shot majority.
7. **Sips debate (your call):** you asked "why sips? why temp files? what if sips makes it inaccurate?" — explained `sips --resampleWidth 1200` rescales `321→1200` so header `6` vs `3` separates (9px → 14px), but blurs high-res. You decided **delete sips entirely, give files directly to Gemini, no temp files**.
8. **Direct-bytes refactor (you typed, I navigated bite-by-bite):** `gemini.go:14` `func geminiExtract(imagePath string)` → `func geminiExtract(data []byte)` + `mime := http.DetectContentType(data)` + `b64` from `data`, no `os.Open`/`buf`/`Seek`/`ReadAll`. `handlers.go:143` Family C and `172` image: `file.Seek(0, SeekStart); data, _ := io.ReadAll(file); if aiDate, aiItems, aiErr := geminiExtract(data); aiErr == nil { ... st.Add ... return }` — no `os.CreateTemp`, no `sips`, no `os/exec` (removed from imports). `go vet` green on both branches (after fixing `io. SeekStart` space typo and `return out.Date, items, nil` discarding `items` bug at `gemini.go:117`).
9. **Bench re-test with real shots:** `2026-08-24 17.56.22.png` (321×1280 narrow) → tesseract `mismatch 128651 != 133641` (Yorma `4009 4 990,00` weight+amount tail missed, Kefir `4. ([Kefir` bracket) vs Gemini via direct bytes `2023-08-23` (year hallucinated) → year guard `2025-2027` fallback to `2026-08-26`, then retake `2026-08-25 15.55.25.jpg` (359×1280 tighter crop) → Gemini `2026-08-23` correct, 6 rows `133641` (year fix + strict prompt `Read EXACTLY, if blurry return ""`).
10. **Prompt generalization (your call):** `Korzinka cheques` → `any store cheque/receipt in Uzbekistan (any store, any language)` — total label `TO'LOV UCHUN, JAMI, ИТОГО, TOTAL, SUMMA` — you wanted all stores, groceries, pharmacies.
11. **Map heterogeneous JSON (your question):** `map[string]interface{}` = "string keys, any value" — lets `json.Marshal` build Gemini's `{"contents":[{"parts":[{"text":prompt},{"inline_data":{...}}]}]}` without defining 4 nested structs; response side uses strict `gr Candidates` struct.
12. **UI polish start (you said "we will do it together, just navigate"):** `web/static/` already existed, `main.go:20` `Handle("/static/", StripPrefix(...FileServer(Dir("web/static"))))` wired, `web/static/style.css` 40→71 lines with `:root` tokens, card shadows, `pill_manual`/`pill_cheque`, `flash` red banner. `list.html` flash slot `{{if .Flash}}` added but `Source` column still pending.

## Bugs hit & fixed

- `bind: address already in use` — old `go run` still on `:8080`, `lsof -ti :8080 | xargs kill -9` before `go run`
- `prove.env` `.gitignore` merge — `echo ".env" >> .gitignore` on file without trailing newline created `prove.env` line → `git checkout -- .gitignore; printf "\n.env\n" >> .gitignore`
- `gemini.go:4` `import "byte"` → `bytes` typo, `return out.Date, nil, nil` discarding `items` at `117` → `return out.Date, items, nil`, `io. SeekStart` space typo at `handlers.go:172`
- `handlers.go:59-63` `AddExpense` bad amount redirected without `return` → ghost 0-amount row stored + double `WriteHeader` — added `return`
- `list.html:7` `href="../static/style.css"` → `"/static/style.css"` (absolute URL)
- `main` at `fa3fb59` only deleted `gemini.go` but left `geminiExtract` calls → `vet undefined` → `cb8833f` fix removed the two Gemini blocks
- `feature/gemini` Gemini-only commenting left mismatched braces (`// }` vs `}`) → `vet unexpected else` → removed entire 18-line `tesseract` blocks
- `AQ.Ab8...` key `AQ` prefix valid (listed `gemini-2.5-flash`/`gemini-3.6-flash`), but `gemini-1.5-flash` 404 for new users → switched to `gemini-3.6-flash` (bench: `2026-08-24 17.56.22.png` → perfect `133641`)

## Code state at end of session

- **Branches (both pushed to `expense-tracker`):**
  - `main` at `cb8833f` tesseract-only: `internal/web/cheque.go` 5 bites (`^\d+[.,] `, `[-=]`, case-proof, `endMoney` trailing/glued, same-line Kefir), `handlers.go` peek magic-bytes (`%PDF` → `ledongthuc/pdf`, else → `sips 1200` → `tesseract`), `list.html` flash slot, `web/static/style.css` upgraded, no `gemini.go` — `go vet` green, `git status` clean, `.env` gitignored
  - `feature/gemini` at `daec2fb`/`9113d57` Gemini-only: `gemini.go:14` `func geminiExtract(data []byte)` direct bytes, any-store prompt, `handlers.go:142-170` direct `data, _ := io.ReadAll(file)` → `geminiExtract(data)` for both scanned-PDF and image (no `sips`/temp files/`os/exec`), `GEMINI_API_KEY` via `.env` (600) + fallback, `go vet` green
- **DB:** `expense.db` at `~/Documents/Developer/expense-tracker/expense.db`, `23` rows after `2026-08-25 15.55.25.jpg` 6-row Gemini insert (previous `27` → `13` after `GEMINI_API_KEY not set` fallback test, then `23`)
- **Static hosting:** `web/static/` exists, `main.go:20` `Handle("/static/")` wired

## Next steps

1. **Close Milestone 5 docs:** `01 - Milestones.md` row 5 already `✅ DONE + committed (Aug 25)` + checklist now includes sips removal → direct bytes, `02 - Journal.md` needs `2026-08-26` entry (this file is the source)
2. **Polish Milestone 6:** finish `list.html` `Source` column (`<th>Source</th>` + `pill_{{.Source}}`), fix `sumbit` typo, `gofmt -w .`, test flash banner `?err=` appears then disappears
3. **No auth** — per your call, project done at 1-5, `7 Auth` stays `⬜ Post-v1 / Improvement` (bald for this app), future improvements only on `main` or new `feature/*` branches

## Vault side quests

- No new Go note this session (next is `20 - encoding/json`), but `19 - net/http.md:368`/`494`/`644` and `18 - Standard Library.md:472` updated earlier to `milestone 7` after renumbering

---

**Tool verification:** `go vet ./...` green on both branches, `git branch -vv` shows `main cb8833f [expense-tracker/main]` and `feature/gemini 9113d57 [expense-tracker/feature/gemini]`, `git status --short` clean on `feature/gemini` after `daec2fb` sips removal, `ls internal/web/` shows `cheque.go`/`handlers.go` on `main`, plus `gemini.go` on `feature/gemini`.
